"use client";

// Kept as a compatibility boundary for earlier imports. Role selection is now
// available only through the clearly separated demo entry on /login.
export default function RoleSwitcher() {
  return null;
}
